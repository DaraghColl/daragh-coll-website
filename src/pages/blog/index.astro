---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import BlogCard from '@components/blog/blog-card.astro';

const blogPosts = await getCollection('blog');
---

<Layout title="Daragh Coll" description="Blog" image="/og-images/blog-home.png">
	<div class="py-24 sm:py-32">
		<div class="container-md px-6 lg:px-8">
			<div class="mx-auto max-w-2xl lg:mx-0">
				<h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
					All things development
				</h2>
				<p class="mt-2 text-lg leading-8 text-gray-400">Latest Articles</p>
			</div>
			<div
				class="mx-auto mt-4 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 border-t border-gray-200 pt-10 lg:mx-0 lg:max-w-none lg:grid-cols-3"
			>
				{
					blogPosts.map((blogPost) => {
						return <BlogCard {...blogPost} />;
					})
				}
			</div>
		</div>
	</div>
</Layout>
